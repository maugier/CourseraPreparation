#include <iostream>
#include <vector>
#include <cmath>
#include <array>
#include <string>

using namespace std;
double ß; // ß represente alpha
double u(exp(-ß*(t-to));
// PROTOTYPAGE DES FONCTIONS 
void  affiche(double temps, sportif p );
double v(double ß, double u);
double h(double ß, double u);


int main() {
	
struct sportif
{
double const g(9.81);
double vo(0);
double to(0);
double ho(39000);
};

sportif felix {
	double masse(80);
	double surface(2.0);
	double altitude(ho);
	double vitesse (vo);
	double acceleration(g);
};

affiche (to, felix)


  return 0;
}

void  affiche(double temps, sportif p )
	{
		cout<< to << "," << ho << ","<< vo << ","<< g << endl;
						}
			
double v(double ß, double u)
{
	v=(g/ß)*(1-u)+vo*u;
	return v;
	}
double h(double ß, double u)
{
	h=ho*(-g/ß)*(t-to)-((vo-(g/ß))/ß)*(1-u);
	return h;
	 }
double h(double ß, double u)
{
	a=g-ß*v;
	return a;
	}
