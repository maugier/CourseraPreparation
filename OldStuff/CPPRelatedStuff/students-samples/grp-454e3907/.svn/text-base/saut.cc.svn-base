#include <iostream>
#include <cmath>
using namespace std;

struct car_Sportif {
	double masse;
	double vitesse;
	double acceleration;
	double altitude;
	double surface;
};

typedef car_Sportif Sportif;
void afficher(double t, Sportif n);
void calculs(Sportif& x, double alpha, double terme);
void evolution(

int main() {
	
	const double g(9.81);
	double vO(0.0), tO(0.0), hO(39000);
	Sportif felix(80, vO, g, hO, 2.0);
	double alpha;
	
	afficher(0, 80, 0, 9.81, 39000, 2.0);
	
	double t;
	double v_t;
	double h_t;
	double a_t;
	double terme;
	
	
	


  return 0;
}

void afficher(double tem, Sportif n) {
	
	cout << tem << ", " << Sportif.altitude << ", " << Sportif.vitesse << ", " << Sportif.acceleration << endl;
	
}

void calculs(Sportif& x, double alpha, double terme) {
	alpha = (Sportif.surface / Sportif.masse);
	terme = exp(-apha*(t-tO));
	
	v_t = (g/alpha)*(1-terme) + vO*terme;
	h_t = hO - (g/alpha)*(t-tO) - ((vO - (g/alpha))/alpha)*(1-terme);
	a_t = g - (alpha*v_t);
}
	
	
