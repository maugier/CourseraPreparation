#include <iostream>
#include <string>
#include <cmath>
using namespace std;

struct Sportif{
	double masse;
	double vitesse;
	double acceleration;
	double altitude;
	double surface;
	};



	//prototypes
	void afficher ();// quand j'essaye de mettre les valeurs en argument ca ne compile pas c'est pourquoi je laisse vide les () 
	void chute_libre();
	void ouverture_parachute();

int main() {

 constexpr double g (9.81);
 double v0 (0.0);
 double t0 (0.0);
 double h0 (39000);
 
 //Sportif felix (80.0,v0,g,h0,2.0); je le commente sinon la compilation ne se fait pas 
 afficher(); // afficherai normalement les donnees concernent le sportif
  return 0;
}

// corps des fonctions
void afficher (){
	double t0 (0.0);
	Sportif s;
cout<<t0<<endl;
cout<<s.altitude<<s.vitesse<<s.acceleration<<endl;
}

void chute_libre()
{double t0 (0.0);
double t;
double alpha;
//double chute (exp(-alpha(t-t0));

}

void ouverture_parachute()
{}
